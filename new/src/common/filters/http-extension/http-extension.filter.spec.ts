import { HttpExtensionFilter } from './http-extension.filter';

describe('HttpExtensionFilter', () => {
  it('should be defined', () => {
    expect(new HttpExtensionFilter()).toBeDefined();
  });
});
